[package]
name = "fabmanager-updater"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.11", features = ["blocking"] }  # Enabling blocking feature
regex = "1"       # For regex operations
semver = "1.0.27"
